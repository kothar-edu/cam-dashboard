# Django API base URL (no trailing slash). Default local Django often runs on port 3030.
VITE_URL=http://127.0.0.1:3030/

# Vite dev server runs on port 5173 (see vite.config.ts) to avoid conflicting with Django.
